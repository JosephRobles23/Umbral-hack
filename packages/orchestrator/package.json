{
  "name": "@umbral/orchestrator",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "gate:plan": "tsx src/gates/run-gate.ts plan",
    "gate:commit": "tsx src/gates/run-gate.ts commit"
  },
  "dependencies": {
    "@umbral/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "typescript": "^5",
    "vitest": "^3"
  }
}
